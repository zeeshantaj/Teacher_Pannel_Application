<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
<!--    <style name="Theme.Teacher_Panel_Application" parent="Theme.MaterialComponents.DayNight.DarkActionBar.Bridge">-->
    <style name="Theme.Teacher_Panel_Application" parent="Theme.Material3.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/darkBlueshade</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>

        <!-- Customize your theme here. -->
    </style>
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/darkBlueshade</item>
    </style>
    <style name="switch_custom_design" parent="Widget.Material3.CompoundButton.MaterialSwitch">
        <item name="colorOnContainerUnchecked">@color/colorOverlay</item>
        <item name="trackTint">@color/darkBlue</item>
        <item name="trackDecorationTint">@color/colorOverlay</item>
    </style>

    <style name="roundImageView" parent="">
    <item name="cornerFamily">rounded</item>
        <item name="cornerSize">15dp</item>
        <item name="color">@color/white</item>
    </style>
    <style name="FullScreenDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>

    </style>
    <style name="CustomBottomSheetDialogTheme" parent="Theme.Design.BottomSheetDialog">
        <!-- Customize your dialog theme here -->
        <item name="android:background">@color/transparent</item>
        <item name="android:windowIsFloating">false</item>
    </style>

    <style name="CustomProgressDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:indeterminateDrawable">@drawable/custom_progress_dialog</item>
        <item name="colorAccent">@color/txt1</item>
    </style>

</resources>